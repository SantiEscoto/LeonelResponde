# Requisitos mínimos para ejecutar API y Voz en Jetson Orin Nano (Ubuntu 22.04 / Python 3.10)
# Objetivo: arrancar FastAPI + Voice WS sin RAG/FAISS ni embeddings.
# Si necesitas RAG, compila e instala faiss desde fuente y PyTorch para aarch64.

# Core API
fastapi==0.104.0
uvicorn==0.24.0
pydantic==2.4.0
colorama==0.4.6

# LLM local vía llama-cpp (CPU/GPU con GGML/CUDA)
llama-cpp-python==0.2.19
huggingface-hub==0.16.4

# Utilidades
numpy==1.24.4        # Pin <2 para compatibilidad con ecosistema en ARM
psutil==5.9.5
GPUtil==1.4.0

# Voz (WebSocket + TTS/STT)
websockets==12.0
vosk==0.3.44
webrtcvad==2.0.10
librosa==0.9.2       # evita conflictos con numpy en ARM
soundfile==0.12.1
sounddevice==0.4.6
scipy==1.10.1
tqdm==4.65.0

# Opcionales (NO se instalan en Jetson por defecto):
# transformers==4.35.2
# sentence-transformers==2.2.2
# faiss-cpu==1.7.4
# TTS==0.22.0        # Coqui XTTS; instalar solo si se desea TTS de alta calidad