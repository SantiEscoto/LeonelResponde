# Dependencias para Sistema de Voz 100% Gratuito
# STT (Speech-to-Text) - 100% Gratuito
openai-whisper==20231117  # Whisper (MIT License)
vosk==0.3.44             # Vosk (Apache 2.0)
webrtcvad==2.0.10        # Voice Activity Detection

# TTS (Text-to-Speech) - 100% Gratuito
# TTS==0.22.0           # Omitido temporalmente por conflictos de numpy/librosa en Py3.9; Coqui XTTS opcional
# tortoise-tts==3.0.0  # Omitido por conflicto con transformers (no requerido para XTTS)      # Tortoise TTS (MIT)
bark==0.1.5              # Bark TTS (MIT)
# marytts==5.2             # MaryTTS (BSD) — no disponible en PyPI estable

# Audio Processing
# pyaudio==0.2.11   # Omitido: requiere PortAudio en sistema (brew install portaudio)
librosa==0.9.2  # Compatibilidad con numpy==1.22.0 para TTS
soundfile==0.12.1
noisereduce==3.0.0
scipy==1.11.4

# WebRTC y Streaming
# aiortc==1.6.0   # Omitido: requiere PyAV/ffmpeg en sistema; no usado por PySide6 UI
websockets==12.0

# Utilities
numpy==1.22.0
pandas==1.5.3
tqdm==4.65.0

